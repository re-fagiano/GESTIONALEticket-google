{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "pnpm install && pnpm prisma:generate && pnpm run build"
  },
  "deploy": {
    "startCommand": "pnpm prisma:deploy && node server.js"
  }
}
